import 'package:flutter/material.dart';

BoxShadow emptyShadow() {
  return const BoxShadow(color: Colors.transparent, offset: Offset.zero, blurRadius: 0.0, spreadRadius: 0.0, blurStyle: BlurStyle.normal);
}